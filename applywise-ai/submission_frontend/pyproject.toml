[project]
name = "submission-frontend"
version = "0.1.0"
description = "ApplyWise AI — Career Review Dashboard"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
